# Mặc định: để trống → frontend gọi /api qua Vite proxy tới http://localhost:5057 (xem vite.config.ts).
# Chỉ set khi bạn KHÔNG dùng proxy (gọi thẳng tới API):
# VITE_API_BASE_URL=http://localhost:5057
